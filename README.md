# Teck Stack Platform

Next.js 14 frontend architecture for `tech-stack.pro`, an AI tools discovery and comparison platform.

## Run Locally

```bash
npm install
npm run dev
```

## Production

```bash
npm run build
npm run start
```

The app uses the Next.js App Router, TypeScript, Tailwind CSS, reusable templates, and static mock data only.
